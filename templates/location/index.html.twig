{% extends 'base.html.twig' %}

{% block title %}Location index{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <div class="col-12">
            <h1>Liste des lieux</h1>
        </div>
        <div class="col-12">
            <table class="table">
                <thead>
                    <tr>
                        <th>Id</th>
                        <th>Name</th>
                        <th>actions</th>
                    </tr>
                </thead>
                <tbody>
                {% for location in locations %}
                    <tr>
                        <td>{{ location.id }}</td>
                        <td>{{ location.name }}</td>
                        <td>
                            <a href="{{ path('location_show', {'id': location.id}) }}"><button class="btn btn-primary">Afficher</button></a>
                            <a href="{{ path('location_edit', {'id': location.id}) }}"><button class="btn btn-success">Editer</button></a>
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="3">no records found</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
    <div class="row">
        <div class="col-12">
            <a href="{{ path('location_new') }}"><button class="btn btn-success">Ajouter un lieu</button></a>
        </div>
    </div>
</div>

{% endblock %}
