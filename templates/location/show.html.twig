{% extends 'base.html.twig' %}

{% block title %}Location{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <div class="col-12">
            <h1>Location</h1>
        </div>
        <div class="col-12">
            <table class="table">
                <tbody>
                    <tr>
                        <th>Id</th>
                        <td>{{ location.id }}</td>
                    </tr>
                    <tr>
                        <th>Name</th>
                        <td>{{ location.name }}</td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>
    <div class="row">
        <div class="col-12 text-right">
            <a href="{{ path('location_edit', {'id': location.id}) }}"><button class="btn btn-success">Editer</button></a>
        </div>
        <div class="col-12"><hr></div>
        <div class="col-6">
            <a href="{{ path('location_index') }}"><button class="btn btn-primary">retour aux lieux</button></a>
        </div>
        <div class="col-6 text-right">
            {{ include('location/_delete_form.html.twig') }}
        </div>
    </div>
</div>




{% endblock %}
