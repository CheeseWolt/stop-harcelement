{% extends 'base.html.twig' %}

{% block title %}Lieu{% endblock %}

{% block body %}
<div class="container theme-container text-theme">
    <br>
    <div class="row">
        <div class="col-12 text-center">
            <h1>Lieu</h1>
            <br>
        </div>
    </div>
    <br>
    <div class="row">
        <div class="col-12 text-center">
            <table class="table">
                <tbody>
                    <tr>
                        <th class="theme-d3">Lieu</th>
                        <td>{{ location.name }}</td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>
    <div class="row">
        <div class="col-12 text-right">
            <a href="{{ path('location_edit', {'id': location.id}) }}"><button class="btn theme-action">Editer</button></a>
        </div>
        <div class="col-12"><hr></div>
        <div class="col-6 text-left">
            <a href="{{ path('location_index') }}"><button class="btn theme-action">retour aux lieux</button></a>
        </div>
        <div class="col-6 text-right">
            {{ include('location/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
