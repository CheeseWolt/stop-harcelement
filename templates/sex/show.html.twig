{% extends 'base.html.twig' %}

{% block title %}Sex{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <div class="col-12">
            <h1>Sex</h1>
        </div>
        <div class="col-12">
            <table class="table">
                <tbody>
                    <tr>
                        <th>Id</th>
                        <td>{{ sex.id }}</td>
                    </tr>
                    <tr>
                        <th>Name</th>
                        <td>{{ sex.name }}</td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>
    <div class="row">
        <div class="col-12 text-right">
            <a href="{{ path('sex_edit', {'id': sex.id}) }}"><button class="btn btn-success">Editer</button></a>
        </div>
        <div class="col-12"><hr></div>
        <div class="col-6">
            <a href="{{ path('sex_index') }}"><button class="btn btn-primary">retour aux sexes</button></a>
        </div>
        <div class="col-6 text-right">
            {{ include('sex/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
