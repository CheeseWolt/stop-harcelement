{% extends 'base.html.twig' %}

{% block title %}Type d'alerte{% endblock %}

{% block body %}
<div class="container theme-container ">
    <br>
    <div class="row">
        <div class="col-12 text-center">
            <h1 class="text-theme">Type d'alerte</h1>
            <br>
        </div>
    </div>
    <div class="row">
        <div class="col-12 text-center">
            <table class="table">
                <tbody>
                    <tr>
                        <th class="theme-d3">Type d'alerte</th>
                        <td>{{ alert_style.name }}</td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>
    <div class="row">
        <div class="col-12 text-right">
            <a href="{{ path('alert_style_edit', {'id': alert_style.id}) }}" class="btn theme-action">Editer</a>
        </div>
        <div class="col-12"><hr></div>
        <div class="col-6 text-left">
            <a href="{{ path('alert_style_index') }}" class="btn theme-action">retour aux types d'alerte</a>
        </div>
        <div class="col-6 text-right">
            {{ include('alert_style/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
