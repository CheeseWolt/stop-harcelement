{% extends 'base.html.twig' %}

{% block title %}AlertStyle{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <div class="col-12">
            <h1>AlertStyle</h1>
        </div>
        <div class="col-12">
            <table class="table">
                <tbody>
                    <tr>
                        <th>Id</th>
                        <td>{{ alert_style.id }}</td>
                    </tr>
                    <tr>
                        <th>Name</th>
                        <td>{{ alert_style.name }}</td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>
    <div class="row">
        <div class="col-12 text-right">
            <a href="{{ path('alert_style_edit', {'id': alert_style.id}) }}"><button class="btn btn-success">Editer</button></a>
        </div>
        <div class="col-12"><hr></div>
        <div class="col-6">
            <a href="{{ path('alert_style_index') }}"><button class="btn btn-primary">retour aux types</button></a>
        </div>
        <div class="col-6 text-right">
            {{ include('alert_style/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
