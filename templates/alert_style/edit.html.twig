{% extends 'base.html.twig' %}

{% block title %}Modifier un type d'alerte{% endblock %}

{% block body %}
<div class="container theme-container text-theme">
    <br>
    <div class="row">
        <div class="col-12 text-center">
            <h1>Modifier un type d'alerte</h1>
        </div>
    </div>
    <br>
    <div class="row">
        <div class="col-12 text-right">
            {{ include('alert_style/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
        </div>
    </div>
    <hr>
    <div class="row">
        <div class="col-6 text-left">
            <a href="{{ path('alert_style_index') }}" class="btn theme-action">retour aux types d'alerte</a>
        </div>
        <div class="col-6 text-right">
            {{ include('alert_style/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
