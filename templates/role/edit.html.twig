{% extends 'base.html.twig' %}

{% block title %}Edit Role{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <div class="col-12">
            <h1>Edit Role</h1>
        </div>
    </div>
    <div class="row">
        <div class="col-12">
            {{ include('role/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
        </div>
    </div>
    <div class="row">
        <div class="col-12">
            <a href="{{ path('role_index') }}"><button class="btn btn-primary">retour aux roles</button></a>
        </div>
    </div>
    <div class="row">
        <div class="col-12">
            {{ include('role/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
