{% extends 'base.html.twig' %}

{% block title %}ClassName{% endblock %}

{% block body %}
    <h1>ClassName</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ class_name.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ class_name.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('class_name_index') }}">back to list</a>

    <a href="{{ path('class_name_edit', {'id': class_name.id}) }}">edit</a>

    {{ include('class_name/_delete_form.html.twig') }}
{% endblock %}
