{% extends 'base.html.twig' %}

{% block title %}Modifier une classe{% endblock %}

{% block body %}
<div class="container theme-container text-theme">
    <br>
    <div class="row">
        <div class="col-12 text-center">
            <h1 class="">Modifier une classe</h1>
        </div>
    </div>
    <br>
    <div class="row">
        <div class="col-12 text-right">
            {{ include('class_name/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
        </div>
    </div>
    <hr>
    <div class="row">
        <div class="col-6">
            <a href="{{ path('class_name_index') }}"><button class="btn theme-action">retour aux classes</button></a>
        </div>
        <div class="col-6 text-right">
            {{ include('class_name/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
