{% extends 'base.html.twig' %}

{% block title %}Edit Status{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <div class="col-12">
            <h1>Edit Status</h1>
        </div>
    </div>
    <div class="row">
        <div class="col-12">
            {{ include('status/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
        </div>
    </div>
    <div class="row">
        <div class="col-12">
            <a href="{{ path('status_index') }}"><button class="btn btn-primary">retour aux status</button></a>
        </div>
    </div>
    <div class="row">
        <div class="col-12">
            {{ include('status/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
