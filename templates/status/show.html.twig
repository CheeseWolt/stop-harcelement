{% extends 'base.html.twig' %}

{% block title %}Status{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <div class="col-12">
            <h1>Status</h1>
        </div>
        <div class="col-12">
            <table class="table">
                <tbody>
                    <tr>
                        <th>Id</th>
                        <td>{{ status.id }}</td>
                    </tr>
                    <tr>
                        <th>Name</th>
                        <td>{{ status.name }}</td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>
    <div class="row">
        <div class="col-12 text-right">
            <a href="{{ path('status_edit', {'id': status.id}) }}"><button class="btn btn-success">Editer</button></a>
        </div>
        <div class="col-12"><hr></div>
        <div class="col-6">
            <a href="{{ path('status_index') }}"><button class="btn btn-primary">retour aux status</button></a>
        </div>
        <div class="col-6 text-right">
            {{ include('status/_delete_form.html.twig') }}
        </div>
    </div>
</div>




{% endblock %}
