{% extends 'base.html.twig' %}

{% block title %}Modifier un niveau scolaire{% endblock %}

{% block body %}
    <div class="container theme-container text-theme">
        <br>
        <div class="row">
            <div class="col-12 text-center">
                <h1>Modifier un niveau scolaire</h1>
            </div>
        </div>
        <br>
        <div class="row">
            <div class="col-12">
                {{ include('class_level/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
            </div>
        </div>
        <hr>
        <div class="row">
            <div class="col-6 text-left">
                <a href="{{ path('class_level_index') }}" class="btn theme-action">retour aux niveaux scolaires</a>
            </div>
            <div class="col-6 text-right">
                {{ include('class_level/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
