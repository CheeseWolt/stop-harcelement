{% extends 'base.html.twig' %}

{% block title %}Niveau scolaire{% endblock %}

{% block body %}
<div class="container">
    <br>
    <div class="row">
        <div class="col-12 text-center">
            <h1>Niveau scolaire</h1>
            <br>
        </div>
    </div>
    <div class="row">
        <div class="col-12 text-center">
            <table class="table">
                <tbody>
                    <tr>
                        <th>Id</th>
                        <td>{{ class_level.id }}</td>
                    </tr>
                    <tr>
                        <th>Niveau scolaire</th>
                        <td>{{ class_level.name }}</td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>
    <div class="row">
        <div class="col-12 text-right">
            <a href="{{ path('class_level_edit', {'id': class_level.id}) }}"><button class="btn btn-success">Editer</button></a>
        </div>
        <div class="col-12"><hr></div>
        <div class="col-6">
            <a href="{{ path('class_level_index') }}"><button class="btn btn-primary">retour aux niveaux scolaires</button></a>
        </div>
        <div class="col-6 text-right">
            {{ include('class_level/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
