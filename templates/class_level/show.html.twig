{% extends 'base.html.twig' %}

{% block title %}Niveau scolaire{% endblock %}

{% block body %}
<div class="container theme-container text-theme">
    <br>
    <div class="row">
        <div class="col-12 text-center">
            <h1>Niveau scolaire</h1>
        </div>
    </div>
    <br>
    <div class="row">
        <div class="col-12 text-center">
            <table class="table">
                <tbody>
                    <tr>
                        <th class="theme-d3">Id</th>
                        <td>{{ class_level.id }}</td>
                    </tr>
                    <tr>
                        <th class="theme-d3">Niveau scolaire</th>
                        <td>{{ class_level.name }}</td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>
    <div class="row">
        <div class="col-12 text-right">
            <a href="{{ path('class_level_edit', {'id': class_level.id}) }}" class="btn theme-action">Editer</a>
        </div>
        <div class="col-12"><hr></div>
        <div class="col-6">
            <a href="{{ path('class_level_index') }}" class="btn theme-action">retour aux niveaux scolaires</a>
        </div>
        <div class="col-6 text-right">
            {{ include('class_level/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
