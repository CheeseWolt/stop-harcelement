{% extends 'base.html.twig' %}

{% block title %}ClassLevel{% endblock %}

{% block body %}
    <h1>ClassLevel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ class_level.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ class_level.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('class_level_index') }}">back to list</a>

    <a href="{{ path('class_level_edit', {'id': class_level.id}) }}">edit</a>

    {{ include('class_level/_delete_form.html.twig') }}
{% endblock %}
