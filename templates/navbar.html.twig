<nav class="navbar navbar-expand-lg navbar-dark fixed-top theme-d3">
    <a class="navbar-brand" href="/"><img src="{{ asset("assets/images/logo-75.png") }}" width="10%"/>
        Stop-Harcelement</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarCollapse">
        <ul class="navbar-nav mr-auto">
            {% if app.user %}
                <li class="nav-item">
                    <a class="nav-link" href="/profil">Mon compte</a>
                </li>
            {% endif %}
            <li class="nav-item">
                <a class="nav-link" href="/usefull/contact">Liens utiles</a>
            </li>
        </ul>
        {% if app.user %}
            <div class="row navbar-nav">
                <div class="btn-group btnNav imgcenter">
                    {% if app.user.role.name == "ROLE_ADMIN" %}
                        <button type="button" class="btn theme-action dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Mes actions possibles</button>
                        <div class="dropdown-menu theme-action dropdown-shadow">
                            {# <a class="dropdown-item" href="/sex">gerer Sexes</a> #}
                            {# <a class="dropdown-item" href="/role">gerer Roles</a> #}
                            {# <a class="dropdown-item" href="/status">gerer status</a> #}<a class="dropdown-item" href="/class/level"> gerer rangs de classe</a>
                            <a class="dropdown-item" href="/location">gerer lieux</a>
                            <a class="dropdown-item" href="/alertstyle">gerer types d'alerte</a>
                            <a class="dropdown-item" href="/class/name">gerer les classes</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" href="/user">gerer utilisateurs</a>
                        </div>
                    {% endif %}
                </div>
                <div class="btn-group btnNav imgcenter">
                    <a href="{{path('infospersos')}}">
                        <button class="btn theme-action">Mes infos persos</button>
                    </a>
                </div>
                <div class="btn-group btnNav imgcenter">
                    <a href="{{path('app_logout')}}">
                        <button class="btn theme-action">Deconnexion</button>
                    </a>
                </div>
            </div>
        {% else %}
            <div class="row">
                <div class="btn-group btnNav col-12">
                    <a href="{{path('app_login')}}">
                        <button class="btn theme-action">Se connecter</button>
                    </a>
                </div>
            </div>
        {% endif %}
    </div>
</nav>
