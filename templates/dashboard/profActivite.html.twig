<script>
   var chart = Highcharts.chart('profs', {

    title: {
        text: 'Activité des professeurs dans le mois'
    },

    xAxis: {
        categories: [
                {% if nac is defined %}
                    {% for profName, nb in pa %}
                        "{{ profName }}",
                    {% endfor %}
                {% endif %}
        ]
    },

    series: [{
        type: 'column',
        name: "nombre de suivis ",
        colorByPoint: true,
        data: [
                {% if nac is defined %}
                {% for nb in pa %}
                   {{ nb }},
                {% endfor %}
            {% endif %}
        ],
        showInLegend: false
    }]

    });


</script>
