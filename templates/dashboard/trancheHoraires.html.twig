<script>
Highcharts.chart('trancheHoraire', {
  chart: {
    plotBackgroundColor: null,
    plotBorderWidth: null,
    plotShadow: false,
    type: 'pie'
  },
  title: {
    text: 'Tranches horaires des incidents'
  },
  tooltip: {
    pointFormat: '{h.heure}: <b>{point.percentage:.1f}%</b>'
  },
  plotOptions: {
    pie: {
      allowPointSelect: true,
      cursor: 'pointer',
      dataLabels: {
        enabled: true,
        format: '{point.name}'
      }
    }
  },
  series: [{
    name: 'Brands',
    colorByPoint: true,
    data: [
    {% if hours is defined %}
      {% for h in hours %}
      { 
        name: "{{ h.heure }}h",
        y: {{ h.nbAlert }}
      },
      {% endfor %}
    {% endif %}
    ]
  }]
});
</script>