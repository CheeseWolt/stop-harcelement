<script>
   var chart = Highcharts.chart('casClasse', {

    title: {
        text: 'Nombre de cas par niveau d\'étude'
    },

    xAxis: {
        categories: [
                {% if nac is defined %}
                    {% for level, nb in nac %}
                        "{{ level }}",
                    {% endfor %}
                {% endif %}
        ]
    },

    series: [{
        type: 'column',
        name: "nombre de cas ",
        colorByPoint: true,
        data: [
                {% if nac is defined %}
                {% for nb in nac %}
                    {{ nb }},
                {% endfor %}
            {% endif %}
        ],
        showInLegend: false
    }]

    });

</script>