{% extends 'base.html.twig' %}

{% block title %}Modifier un utilisateur{% endblock %}

{% block body %}
<div class="container">
    <br>
    <div class="row">
        <div class="col-12">
            {{ include('user/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
        </div>
    </div>
    <div class="row">
        <div class="col-12">
            <a href="{{ path('profil_index') }}"><button class="btn btn-primary">retour au profil</button></a>
        </div>
    </div>
    <div class="row">
        <div class="col-12">
            {{ include('user/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
